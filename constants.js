const SVG_NS = 'http://www.w3.org/2000/svg';
const SVG_SIZE = 1100;
const CENTER = SVG_SIZE / 2;
const LEVELS_WIDTHS = [45, 40, 70, null, 125];
const DEFAULT_LEVEL_WIDTH = 115;
const FONT_SIZE = 14;

export {
    SVG_NS,
    SVG_SIZE,
    CENTER,
    LEVELS_WIDTHS,
    DEFAULT_LEVEL_WIDTH,
    FONT_SIZE,
};