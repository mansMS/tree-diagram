const SVG_NS = 'http://www.w3.org/2000/svg';
const SVG_SIZE = 1100;
const CENTER = SVG_SIZE / 2;
const DEFAULT_FONT_SIZE = 14;

export {
    SVG_NS,
    SVG_SIZE,
    CENTER,
    DEFAULT_FONT_SIZE,
};